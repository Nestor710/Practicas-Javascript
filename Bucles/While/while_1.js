/*var nombre, apellido, edad, poblacion;

nombre=prompt("introduce tu nombre por favor")

while (!isNaN(nombre)) {

    nombre=prompt("has introducido un nombre invalido");

}

apellido=prompt("Introduce tu apellido por favor");

while (apellido!="Lima") {

    apellido=prompt("has introducido un apellido invalido");
     
}

 edad=prompt("introduce tu edad por favor");

while (edad<=0 || edad>=100 || isNaN(edad)) {

    edad=prompt("introduce una edad valida");
     
}

poblacion=prompt("introduce tu poblacion");

while (!isNaN(poblacion)) {

    poblacion=prompt("introduce una poblacion valida");

}
*/

let x=0;

while (x<=10) {
    document.write(x);
    x++;
}
